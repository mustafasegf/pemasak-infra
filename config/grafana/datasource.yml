apiVersion: 1

datasources:
- name: Prometheus
  type: prometheus
  url: http://localhost:9090 
  access: proxy
  editable: true
  isDefault: true

- name: Loki
  type: loki
  access: proxy
  url: http://localhost:3100
